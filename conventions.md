
##  variables and constants
```c
int g_globalVariable (g_ for global followed by camelCase)
int localVariable (camelCase)

const CONSTANTS (SCREAMING_SNAKE_CASE)
#DEFINE MACROS (SCREAMING_SNAKE_CASE)

enum EnumName (PascalCase)
ENUM_ITEM (SCREAMING_SNAKE_CASE)
```
## funcs and data
```c
FunctionName() (PascalCase)
typedef struct StructName (PascalCase)
union UnionName

```
